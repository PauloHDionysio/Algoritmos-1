#include <stdio.h>
#include <string.h>

typedef struct{
    char nome[21];
}string;

int main(){
    string aluno;
    int K, N;
        scanf("%d %d", &N, &K);
            for(int i = 0; i < N; i++){
                scanf(" %s", &nome[i]);
            }


return 0;    
}